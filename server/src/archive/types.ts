export interface WarArchiveEvent { id: number; timestamp: string; eventType: string; severity: string; planetId: number | null; planetName: string | null; title: string; summary: string; data: unknown; }
export interface CampaignProgressSample { timestamp: string; planetId: number; planetName: string; campaignType: string; faction: string; healthCurrent: number; healthMax: number; liberationPercent: number; decayPerHourHp: number | null; playerShare: number; playersOnPlanet: number; hoursRemaining: number | null; rampingUp: boolean; isHighPriority: boolean; liberationDeltaPerHour: number | null; healthDeltaPerHour: number | null; playerShareDelta: number; playersDelta: number; }
export interface ArchiveSummary { available: boolean; enabled: boolean; totals: { campaignSamples: number; events: number; planets: number; majorOrders: number }; latest: { campaignSampleAt: string | null; eventAt: string | null }; recentEvents: WarArchiveEvent[]; }
