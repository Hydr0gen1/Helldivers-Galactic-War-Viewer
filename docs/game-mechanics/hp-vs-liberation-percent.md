# HP vs Liberation %

The wiki formula for displayed liberation percentage is:

`liberationPercent = (maxHealth - health) / maxHealth`

A common failure mode is projecting ETA from percent complete. Different planets can have very different max HP (including regional HP extensions).

Always compute strategic estimates from raw HP and time-series deltas.

