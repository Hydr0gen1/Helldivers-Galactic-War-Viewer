# HP vs Liberation %

A common failure mode is projecting ETA from percent complete. Different planets can have very different max HP.

Always compute strategic estimates from raw HP and time-series deltas.
